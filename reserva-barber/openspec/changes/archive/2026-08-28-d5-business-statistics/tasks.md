## 1. Pure rules — tests first, no I/O

- [x] 1.1 Write failing tests extending `src/server/domain/models/bookingCalendar.test.ts` for `weekBoundsOf`: a Monday resolves to its own week; a Sunday resolves to the week that began six days earlier; the interval is half-open (`[Monday 00:00, next Monday 00:00)`); both boundaries land on the business's midnight, not the runtime's; a week spanning a month end and a week spanning a year end both carry correctly (design D7).
- [x] 1.2 Write failing tests for `previousMonth`: January returns the preceding December with the year decremented; a 31st does not become an invalid date in a 30-day month; `monthBoundsOf(previousMonth(today))` covers the whole calendar month rather than a fixed span.
- [x] 1.3 Implement `weekBoundsOf` and `previousMonth` in `src/server/domain/models/bookingCalendar.ts`, built on `localToInstant` and `addDays`, computed from **both** boundaries rather than one plus a duration (design D7). Introduce none of the banned runtime calendar readers — `businessTime.test.ts` scans this directory and must stay green.
- [x] 1.4 Write failing tests for `src/server/domain/models/statistics.test.ts` covering `averageDepositPerBooking`: zero appointments yields an absent average, never `"0.00"`; appointments with no approved deposits yields exactly `"0.00"`, not an absent value; a quotient of exactly half a centavo rounds up; no floating-point intermediate exists at any step.
- [x] 1.5 Implement `src/server/domain/models/statistics.ts` — `BusinessStatistics`, the range slug tuple and its derived union, and `averageDepositPerBooking` over integer cents using `money.ts`'s existing helpers (design D8). State in the header why the division is here and not in SQL, citing the two disagreeing branches of `toCanonicalDecimal`.
- [x] 1.6 Write failing tests for `src/server/application/dashboard/statisticsRangeParams.test.ts`: each of the six slugs resolves to itself; an unknown, empty, non-canonical or over-length value degrades to `hoy`; a repeated parameter takes its first occurrence; `statisticsRangeHref('hoy')` carries no parameter while the other five do; and no input shape throws (design D6).
- [x] 1.7 Implement `STATS_RANGE_PARAM`, `STATISTICS_RANGES`, `resolveStatisticsRange`, `intervalFor` and `statisticsRangeHref` in `src/server/application/dashboard/statisticsRangeParams.ts`, following `recentBookingsParams.ts` and `clientPageParams.ts` — bound the length, match against the closed set, degrade, never parse (design D6).
- [x] 1.8 Write a failing test asserting that `intervalFor` is the only path from a slug to an interval, and that it returns instants rather than any value that could be interpolated into SQL (design D5).

## 2. The read

- [x] 2.1 Define `IStatisticsRepository` in `src/server/domain/repositories/IStatisticsRepository.ts` with `readStatistics({ ownerId, range })`, plus the header stating the invariants: owner scope reaches through `barber → location → ownerId` and **is** the tenancy boundary; isolation is proven by a two-owner fixture in both directions; the range arrives already computed because that conversion is a domain rule; money crosses as canonical decimal strings; counts are narrowed from the driver's wide integer type here; the statement narrows and does not decide — no clause reads `holdExpiresAt` (design D3).
- [x] 2.2 Write failing tests in `src/server/infrastructure/prisma/PrismaStatisticsRepository.test.ts` against a mocked client: exactly one query is issued; the owner predicate is present in both the outer statement and the income sub-query; `Payment` is **not** joined into the counted row set; the mapping narrows `bigint` counts to `number` and routes the sum through `toCanonicalDecimal`; the returned shape carries no client name, email, telephone, booking id or token. Record in the file that a mock proves the call was made and never that the driver accepts its result types (T58).
- [x] 2.3 Implement `PrismaStatisticsRepository` — **one `$queryRaw`**, `count(*) FILTER (…)` for appointments and cancellations, `count(DISTINCT b."clientId") FILTER (…)` for unique clients, the `cancelledBy` breakdown as two more `FILTER` clauses, and income as a **correlated sub-query carrying its own owner predicate** (design D4, D12). Pass two `Date` parameters and the owner id; compute no dates in SQL and use no `date_trunc` (design D5).
- [x] 2.4 Guard the empty result for shape, not for an empty shop: a bare aggregate returns one row of zeros even when the owner has no barbers, and the comment must say so or the next reader will convert it into a failure state.
- [x] 2.5 Extract `Loaded<T>` from `src/server/application/services/DashboardSummaryService.ts` into a shared application module and update both importers (design D11). No second declaration.
- [x] 2.6 Write failing tests for `src/server/application/services/StatisticsService.test.ts`: one `now` is read and every interval derives from it; the resolved range is returned alongside the figures; a failed read returns `{ ok: false }` rather than zeros; the failure is logged through `toErrorLogContext` with an operation name and the context's **keys** are pinned rather than the absence of strings (D4's correction).
- [x] 2.7 Implement `StatisticsService` in `src/server/application/services/StatisticsService.ts`.

## 3. The page

- [x] 3.1 Create `app/(dashboard)/estadisticas/statisticsService.ts` — the composition root: Prisma client and logger, nothing else.
- [x] 3.2 Add a source-level test asserting the composition root's dependencies — no cipher, no storage client, no payment gateway, no Supabase session client, no writer — so the claim in the proposal cannot drift from the code (the C2/D3/D4 pattern).
- [x] 3.3 Add the `statistics` namespace to `src/lib/copy.ts`: heading, the six range labels, the five card labels, the basis sentence on the income card, the average's absent-state explanation, the cancellation breakdown labels, and the three distinct empty/failure states. Spanish (es-AR), nothing inline in a component.
- [x] 3.4 Re-word `dashboard.monthIncomeHelp` in `src/lib/copy.ts` so D1's card states that its deposits are the ones **approved during** the month, and update `app/(dashboard)/(home)/page.test.tsx` for the new assertion (spec delta on `dashboard-home`, design D1).
- [x] 3.5 Build `app/(dashboard)/estadisticas/layout.tsx` carrying the range control as six `<Link>`s with `prefetch={false}`, hoisted above the suspense boundary so it survives the loading state (design D9, D10).
- [x] 3.6 Resolve D9's open half during implementation: whether the hoisted control can mark its current item without becoming a Client Component. If it cannot, move the control back into the page, accept the flash, and record which way it landed and why — in `design.md` and in the roadmap entry. Do not leave it undocumented.
- [x] 3.7 Build `app/(dashboard)/estadisticas/page.tsx`: `requireOwner()` first and in its own right, `dynamic = 'force-dynamic'`, `robots: { index: false, follow: false }`, the five figures as a `<dl>`, the cancellation breakdown rendered only when non-zero, and all `Intl` formatting on the server.
- [x] 3.8 Implement the three distinct states — a quiet period naming the selected range and offering a wider one, a shop with no bookings at all offering the public profile route, and a failure state replacing the whole figure region. Never sharing copy, never rendering a zero on a failed read.
- [x] 3.9 Render the average as a dash with its explanation when absent, and `Cancelaciones: 0` as a plain zero. Add a test pinning the asymmetry, because it will read as an inconsistency to anyone who has not read the spec.
- [x] 3.10 Add `app/(dashboard)/estadisticas/loading.tsx`, shaped like the card grid and covering only the figures.
- [x] 3.11 Write `app/(dashboard)/estadisticas/page.test.tsx` covering: the three states; an unknown `?rango` rendering `Hoy` as current; the absent average; the breakdown hidden at zero; a seven-digit sum wrapping rather than overflowing; and that no monetary value reaches a log call on the failure path.

## 4. The entry point

- [x] 4.1 Add the `/estadisticas` nav entry to `app/(dashboard)/layout.tsx`, after `Clientes`.
- [x] 4.2 Assert the nav entry renders and routes correctly.
- [x] 4.3 Add a test pinning that `/estadisticas` is protected by the deny-by-default route guard without a new entry in `decideGuardAction` — the property is invisible and a future "convenience" prefix would silently open it.

## 5. Verification against the live database

- [x] 5.1 Run the documented `repeat('x', 1400)` payload check from T68 **before** writing the gate, and record the result. The fault is intermittent, so a green gate is not evidence it is gone and a hanging gate is not evidence of a product defect. This story's payload is small by construction — confirm that rather than assume it.
- [x] 5.2 Write `scripts/d5-gate.ts` with a two-owner fixture, copying `probeOrSkip` from `scripts/d3-gate.ts`. Probes: cross-owner isolation in **both** directions including the income sum specifically; an `APPROVED` payment on an `EXPIRED` booking excluded from income; the same on a `CANCELLED` booking; a booking with two `REJECTED` plus one `APPROVED` payment counted exactly once in every figure; a `PENDING_PAYMENT` hold counted nowhere; an `EXPIRED` booking counted nowhere; the `cancelledBy` breakdown across `OWNER`, `CLIENT` and a null; one client with three confirmed bookings counted once; a booking at exactly `range.start` included and one at exactly `range.end` excluded; a stored `2000.50` returning `"2000.50"`; the absent average over an empty period; and the round-trip count measured through a query extension rather than claimed.
- [x] 5.3 Capture `EXPLAIN (ANALYZE, BUFFERS)` for the real statement against real rows and paste the plan into the roadmap entry. Add an index on `Booking.clientId` **only** if the plan asks for one; if it does not, record that as the deliverable (design D13, T81).
- [x] 5.4 Run the gate, remove its fixture, and record the result — reporting any probe that could not run as **not run**, never as passed.
- [x] 5.5 Drive the page authenticated over HTTP on `next dev` and on `wrangler dev` against the live database, using a marked fixture removed afterwards: the guard, all six ranges, the three states, the absent average, the breakdown, and the phone layout. The raw statement runs through the driver adapter on `workerd`, which is what a mock cannot certify.
- [x] 5.6 Run D3's cheap timezone check — a server whose own calendar has already rolled to the next date (`TZ=Pacific/Kiritimati`) — and confirm `Hoy` still answers for the business's day. Then close the `workerd` half in its real evening window, since that runtime's clock is UTC and ignores `TZ`.

## 6. Quality gates and documentation

- [x] 6.1 Run the full suite, `npm run lint` and `npm run typecheck`; no test skipped, no implicit `any`.
- [x] 6.2 Measure the built Worker's gzip size against `main`'s 3167.86 KiB and record the delta (T51). Confirm no charting or dashboard library entered the bundle — Recharts is D6's cost.
- [x] 6.3 Update `docs/tech-debt.md`: answer **T58** for this call site (its trigger fired and the gate is in the same change); re-measure **T81** with this statement's plan; and open new entries for the late-payment money that now appears on no surface (design D14), the deferred cash-collected figure belonging to D6, and anything the `EXPLAIN` surfaced.
- [x] 6.4 Tick the **D5** entry in `docs/roadmap.md` with the decisions that were actually made, the measurements that were actually taken, and the outcome of D9 — following the register of the D3 and D4 entries: what was decided, what was measured, and what turned out to be wrong.
- [x] 6.5 Update `docs/data-model.md` **only** if an index was added, recording the plan that justified it.
- [x] 6.6 Run `openspec validate --changes` and archive the change under `openspec/changes/archive/`.
